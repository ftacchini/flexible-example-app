name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
    - uses: actions/checkout@v4
      with:
        path: flexible-example-app
    - name: Checkout flexible-core
      uses: actions/checkout@v4
      with:
        repository: ftacchini/flexible-core
        path: flexible-core
    - name: Checkout flexible-decorators
      uses: actions/checkout@v4
      with:
        repository: ftacchini/flexible-decorators
        path: flexible-decorators
    - name: Checkout flexible-http
      uses: actions/checkout@v4
      with:
        repository: ftacchini/flexible-http
        path: flexible-http
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install and build flexible-core
      working-directory: flexible-core
      run: |
        npm install
        npm run build
    - name: Install and build flexible-decorators
      working-directory: flexible-decorators
      run: |
        npm install ../flexible-core
        npm run build
    - name: Install and build flexible-http
      working-directory: flexible-http
      run: |
        npm install ../flexible-core
        npm run build
    - name: Install, build, and test flexible-example-app
      working-directory: flexible-example-app
      run: |
        npm install ../flexible-core ../flexible-decorators ../flexible-http
        npm run build --if-present
        npm run test
      env:
        CI: true
